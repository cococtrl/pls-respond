import React from 'react';
import styles from './Events.module.css';

const Events = (props) => {
    return (
        <main>
            <h1>Events</h1>
        </main>
    )
}

export default Events;